# Public URL of the API server; the SPA's auth client talks to it.
VITE_API_URL=http://localhost:4180

# Future flag: when set, the SPA passes its session to a browser-extendable
# OAuth/SSO provider. Currently unused — auth is Better Auth over the API.
# VITE_PUBLIC_SSO_URL=